const sets = require('../sets');

let out = `// This file is autogenerated by "make build_types", do not edit it.

import React from 'react';
import PreviewSet from './PreviewSet';
import type { Meta, StoryObj } from '@storybook/react';

const meta = {
  title: 'Icon sets',
} satisfies Meta;

export default meta;
type Story = StoryObj<typeof meta>;

// export const ant_fill: Story = {
//   name: 'ant_fill',
//   render: () => <PreviewSet set={'ant_fill'} icons={require('../../sets/ant_fill')} />,
// };
`;

const generateStory = (set) => `
export const ${set}: Story = {
  name: '${set}',
  render: () => <PreviewSet set={'${set}'} icons={require('../../sets/${set}')} />,
};
`;

for (let i = 0; i < sets.length; i++) {
  const set = sets[i];
  out += generateStory(set);
}

require('fs').writeFileSync('src/__story__/demo.stories.tsx', out);
